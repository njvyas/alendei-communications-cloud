import { Card, CardDescription, CardTitle } from '@/components/ui/card';

export default function ConsoleLoading() {
  return (
    <Card>
      <CardTitle>Loading</CardTitle>
      <CardDescription>Fetching console data…</CardDescription>
    </Card>
  );
}
